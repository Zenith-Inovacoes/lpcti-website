import styled from "styled-components";

export const Carrossel = styled.div`
  max-width: 90vw;
  display: flex;
  overflow-x: auto;
  scroll-behavior: smooth;
  flex: none;
  ::-webkit-scrollbar{
    
  }
`;
